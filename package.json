{
  "name": "resonancedb-monorepo",
  "version": "0.1.0",
  "description": "ResonanceDB stream processing database - monorepo structure",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun test packages/core/tests/",
    "test:core": "cd packages/core && bun test",
    "dev:demo": "cd packages/demo && bun run dev",
    "build:demo": "cd packages/demo && bun run build",
    "install:all": "bun install"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "resolutions": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}